fastapi
uvicorn
pydantic
python-dotenv
requests

# Database (keep only if you use MongoDB)
pymongo
motor

# Auth (keep if used)
python-jose
passlib[bcrypt]

# AI / APIs (keep only what you actually use)
openai
google-generativeai